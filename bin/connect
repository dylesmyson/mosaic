#!/usr/bin/env python3

from lib import *
from sys import argv

if __name__ == '__main__':
    name = argv[1] if len(argv) > 1 else 'mosaic'

    session = Session(name)
    print('Session connected!')
    print('Call `help(session)` for help.')
